# TODO

## 功能补全

### 批量生成
一次生成多章，适合大纲已定的冲量阶段。

### 风格模仿
上传一段参考文本，AI 学习后按该风格生成。

### 敏感词检测
生成后/发布前自动扫描，提示风险段落。

### 导出格式增强
支持 EPUB、Word（.docx），不仅仅是 TXT/PDF。

### 多语言界面
界面支持英文，拓宽用户群。

### 深色模式
支持深色模式切换。

---

## 性能 & 稳定性

### 流式输出优化
当前 SSE 逐字输出，可加打字机动效更流畅。

### Redis 缓存
相同概要的生成结果缓存，避免重复消耗 token。

### PostgreSQL
SQLite 并发写入差，用户多了会遇到锁表。

### 异步任务队列
生成多章时后台异步，前端可做进度条。

---

## 写作体验优化

### 章节大纲管理
支持思维导图式的大纲编辑，拖拽排序，层级结构。

### 写作目标与统计
每日字数目标、写作时长统计、连续写作天数。

### 查找替换
全文/章节内的查找替换，支持正则表达式。

### 拼写检查
中文错别字检查、标点符号规范化。

### 朗读功能
AI 朗读章节内容，帮助发现问题。

### 分屏编辑
同时编辑两个章节，方便对照。

---

## AI 功能增强

### AI 对话历史
保存与 AI 的对话历史，可回顾、可继续对话。

### AI 自定义提示词
允许用户保存和管理自定义的 AI 提示词模板。

### 批量 AI 处理
对多个章节批量执行 AI 操作（如批量润色）。

### 角色对话一致性检查
AI 检查角色言行是否符合其设定。

### AI 续写建议
基于当前内容，AI 给出多个续写方向建议。

---

## 协作与分享

### 作品分享
生成分享链接，允许他人只读查看。

### 评论功能
在章节上添加评论、批注。

### 版本对比可视化
更直观的版本差异对比界面（类似 GitHub 的 diff）。

### 协作编辑
多用户协作（需要权限管理）。

---

## AI Agent 深度优化

### 工具调用优化
- 工具描述的语义化：让模型用自然语言描述需求，系统自动匹配工具
- 工具参数的自动填充：根据上下文自动填充部分参数
- 工具组合的优化：让模型可以组合多个工具调用

### 上下文管理优化
- 上下文压缩：让模型（或另一个小模型）压缩工具返回的内容
- 上下文选择：让模型选择保留哪些上下文
- 上下文遗忘：自动遗忘不重要的历史信息

### 模型行为可观测性
- 记录模型的每一次思考和工具调用
- 可视化展示 Agent 的决策过程
- 分析模型行为，用于改进提示词和工具设计

---

## 数据库层优化

### 查询优化
- 为常用查询添加适当的数据库索引
- 优化 N+1 查询问题，使用 `selectinload` 等预加载策略
- 实现分页查询，避免一次加载过多数据

### 连接池
配置数据库连接池，减少连接开销。

### 读写分离
主从复制，读操作走从库。

---

## 缓存层优化

### 多级缓存
本地缓存 + Redis 分布式缓存。

### 缓存预热
启动时预加载热点数据。

### 缓存击穿保护
防止缓存击穿、缓存雪崩。

---

## 前端性能优化

### 代码分割
使用 React.lazy 和 Suspense 实现路由级代码分割。

### 虚拟滚动
对于章节列表等长列表，使用虚拟滚动减少 DOM 节点。

### 图片懒加载
图片懒加载。

### 渲染优化
- 合理使用 React.memo、useMemo 和 useCallback
- 避免不必要的重渲染
- 优化大型组件的拆分

---

## 后端架构优化

### 依赖注入
引入依赖注入容器，简化依赖管理，提高代码可测试性。

### Repository 模式
进一步分离数据访问层。

### 服务层拆分
按领域拆分服务，避免服务过大。

### API 版本控制
为 API 添加版本号，便于后续升级。

### 统一错误处理
更完善的统一错误处理和响应格式。

---

## 前端架构优化

### 状态管理
考虑使用 Zustand 或 Redux Toolkit 替代当前的简单 useContext。

### 组件库
建立可复用的组件库，提高开发效率。

### 类型安全
完善 TypeScript 类型定义，避免使用 any 类型。

---

## 测试与质量

### 单元测试
为核心业务逻辑编写测试用例。

### 集成测试
为 API 端点编写集成测试。

### E2E 测试
使用 Playwright 或 Cypress 编写端到端测试。

### 代码质量工具
引入代码检查工具（如 ESLint、Pylint）、代码格式化（如 Prettier、Black）。

---

## 安全性优化

### 认证与授权增强

#### 密码安全性
- 添加密码强度验证
- 实现密码重置功能

#### JWT 改进
- 实现 token 刷新机制
- 考虑添加 token 黑名单

#### OAuth 登录
支持 Google、GitHub 等第三方登录。

### 输入验证增强

#### 严格验证
对所有用户输入进行严格验证，防止 SQL 注入、XSS 等攻击。

#### CSRF 防护
添加 CSRF token 保护。

### 配置与部署安全

#### 环境变量管理
- 强制要求生产环境设置强 secret_key
- 避免在代码中硬编码敏感信息

#### HTTPS
生产环境强制使用 HTTPS。

#### CORS 配置
生产环境严格限制 CORS 来源。

#### 安全头
添加适当的 HTTP 安全头，如 X-Content-Type-Options、X-Frame-Options 等。

#### 速率限制
防止 API 滥用，保护 LLM 调用不被恶意消耗。

---

## 可观测性优化

### 监控增强

#### 完善 OpenTelemetry
- 添加更多自定义指标
- 实现分布式追踪

#### 应用性能监控
监控关键性能指标，如响应时间、错误率等。

#### LLM 使用监控
更详细的 token 使用分析和预警。

#### 健康检查
添加 API 健康检查端点。

### 日志增强

#### 结构化日志
使用结构化日志格式，便于分析。

#### 日志级别管理
不同环境使用不同的日志级别。

#### 日志收集
考虑使用 ELK 栈或类似方案收集和分析日志。

### 告警增强

#### 关键指标告警
设置合理的告警阈值。

#### 错误告警
及时发现和处理错误。

#### LLM 配额告警
当 token 使用接近配额时提前预警。

---

## 部署与运维

### 容器化

#### Docker 化
后端和前端分别 Docker 化。

#### Docker Compose
本地开发环境一键启动。

#### 多阶段构建
优化镜像大小。

### CI/CD

#### 自动化测试
每次提交自动运行测试。

#### 自动化构建
自动化构建 Docker 镜像。

#### 自动化部署
自动化部署到测试/生产环境。

### 数据备份

#### 定期备份
数据库定期备份。

#### 异地备份
备份存储到异地。

#### 备份恢复验证
定期验证备份可恢复。
